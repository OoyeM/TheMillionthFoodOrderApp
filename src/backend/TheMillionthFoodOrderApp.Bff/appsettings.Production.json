{
  "_note": "Production overrides. AllowedHosts must be set to the public BFF hostname(s) before deploying. DataProtection:KeyPath should resolve to a persistent volume shared across replicas.",
  "AllowedHosts": "bff.themillionthfoodorderapp.com",

  "DataProtection": {
    "KeyPath": "/var/lib/themillionthfoodorderapp/dpkeys"
  },

  "Authentication": {
    "UseMockAuth": false,
    "Cookie": {
      "SlidingExpirationHours": 8
    }
  }
}
